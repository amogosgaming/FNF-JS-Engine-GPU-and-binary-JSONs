#pragma once

// The runtime owns the canonical packed binary schema. Keep this forwarding
// header so the standalone compiler can retain its existing include path.
#include "../../../native/psychgpu/include/note_format.h"
